$black = black
$white = #fff
$size = 50px

.cell
  height $size
  width $size
  display inline-block
  background $white
  border 1px solid black
  &_black
    background $black
  &:hover
    cursor pointer